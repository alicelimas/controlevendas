# Controle de Vendas
Projeto Django para gerenciamento de vendas.

## Como rodar
1. Clone o repositório.
2. Crie um ambiente virtual: `python -m venv venv`.
3. Ative o ambiente: `source venv/bin/activate` (Linux/Mac) ou `venv\Scripts\activate` (Windows).
4. Instale dependências: `pip install -r controlevendas/requirements.txt`.
5. Execute migrações: `python controlevendas/manage.py migrate`.
6. Inicie o servidor: `python controlevendas/manage.py runserver`.
